# Sugarcoat 적용 가이드

Blogger(블로그스팟) 테마입니다. 티스토리 스킨이 아닙니다.

지금 올리는 파일은 하나입니다.

```text
sugarcoat_blogger_theme.xml
```

---

## 백업은 이 한 파일이면 됩니다

방금 올려주신 `theme-7815195283100371423_882f.xml`이 **지금 블로그 테마 전체 백업**입니다.

그걸 컴퓨터에 이 이름으로 저장해 두세요.

```text
blogger-backup-현재테마.xml
```

테마를 바꾼 뒤 마음에 안 들면, Blogger 관리자 → 테마 → 복원에서 이 파일을 다시 올리면 **예전 Easypress 화면으로 돌아갑니다.**

별도로 복사할 것은 거의 없습니다. 다만 테마 HTML에 들어 있던 아래 3개는 새 테마에도 이미 옮겨 두었습니다.

- 네이버 서치어드바이저 소유확인
- Google 애널리틱스 (`G-KDJJ18MT54`)
- AdSense 클라이언트 (`ca-pub-6692939836499331`)
- 사이드바 광고 슬롯 `7633913202`
- 본문 하단 광고 슬롯 `1008345394`

글, 라벨, 페이지, 도메인은 테마와 무관해서 백업하지 않아도 유지됩니다.

---

## 새 테마 적용

1. Blogger 관리자 → **테마** → **복원**
2. 새로 받은 `sugarcoat_blogger_theme.xml` 업로드
3. 미리보기에서 홈 / 글 하나 / 모바일만 보고 **저장**

이전에 `테마를 복원할 수 없습니다`가 나왔다면, 예전 XML을 쓰지 말고 **이 파일을 다시** 올리면 됩니다. Blogger는 위젯 id가 `HTML1`처럼 `종류+숫자`여야 하는데, 예전 파일은 `HTMLAdTop`처럼 잘못된 이름을 쓰고 있었습니다.

복원이 또 실패하면 테마 → 맞춤설정 → **HTML 수정**에서 파일 내용을 통째로 붙여넣고 저장하세요.

저장 후 **레이아웃**에서 블로그 제목이 `핫딜포페어런츠`인지, 상단 메뉴와 사이드바가 보이는지만 확인하면 됩니다.

---

## 적용 후 확인

1. 시크릿 창으로 블로그 홈을 연다
2. 헤더에 easypress / 애드센스팜 문구가 없는지
3. 글 하나에서 제목, 본문, 목차, 댓글이 나오는지
4. 사이드바·본문 하단에 광고가 나오는지
5. 모바일에서 메뉴와 목차 버튼이 열리는지
6. 네이버 서치어드바이저 / 애널리틱스가 계속 잡히는지

빈 AD 박스가 보이면 안 됩니다. 코드가 있는 슬롯만 보여야 합니다.

문제 있으면 백업 파일 `blogger-backup-현재테마.xml`을 다시 복원하면 됩니다.
