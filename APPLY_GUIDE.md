# Sugarcoat 적용 가이드

Blogger(블로그스팟) 테마입니다. 티스토리 스킨이 아닙니다.

지금 올리는 파일은 하나입니다.

```text
sugarcoat_blogger_theme.xml
```

`preview.html`은 로컬에서 레이아웃만 보는 미리보기입니다.

---

## 백업은 이 한 파일이면 됩니다

방금 올려주신 `theme-7815195283100371423_882f.xml`이 **지금 블로그 테마 전체 백업**입니다.

그걸 컴퓨터에 이 이름으로 저장해 두세요.

```text
blogger-backup-현재테마.xml
```

테마를 바꾼 뒤 마음에 안 들면, Blogger 관리자 → 테마 → 복원에서 이 파일을 다시 올리면 **예전 Easypress 화면으로 돌아갑니다.**

별도로 복사할 것은 거의 없습니다. 다만 테마 HTML에 들어 있던 아래 항목은 새 테마에도 이미 옮겨 두었습니다.

- 네이버 서치어드바이저 소유확인
- Google 애널리틱스 (`G-KDJJ18MT54`)
- AdSense 클라이언트 (`ca-pub-6692939836499331`)
- 사이드바 광고 슬롯 `7633913202`
- 본문 하단 광고 슬롯 `1008345394`

글, 라벨, 페이지, 도메인은 테마와 무관해서 백업하지 않아도 유지됩니다.

---

## 새 테마 적용

복원은 지금 블로그의 기존 가젯(`HTML1` 오른쪽 광고 등)과 충돌하면 **아무 설명 없이** 막힙니다. 그래서 이번엔 **HTML 전체 붙여넣기**를 먼저 하세요.

1. 컴퓨터에서 `sugarcoat_blogger_theme.xml`을 메모장으로 연다
2. 전체 선택(Ctrl+A) 후 복사
3. Blogger 관리자 → **테마** → 맞춤설정 옆 ▼ → **HTML 수정**
4. 편집기 안 코드를 전체 선택하고, 복사한 내용을 붙여넣는다
5. **저장**

이번에 나온 `PageList1` / `homeLink` 오류는 새 파일에서 빼 두었습니다. 예전 xml을 다시 붙이지 마세요.

저장이 되면 미리보기에서 홈 / 글 하나 / 모바일만 보면 됩니다. 레이아웃에서 블로그 제목이 `핫딜포페어런츠`인지, 상단 메뉴와 사이드바가 보이는지만 확인하면 됩니다.

HTML 수정도 저장이 안 되면:

1. 테마에서 **기본 테마 하나**를 적용한다 (위젯이 초기화됩니다)
2. 그다음 테마 → **복원**에서 `sugarcoat_blogger_theme.xml`을 올린다

zip이 아니라 **xml 파일**을 쓰세요. 예전 실패 파일은 쓰지 마세요.

---

## 광고 넣는 위치

테마 → 레이아웃에서 아래 가젯이 보입니다. 각 가젯에 AdSense HTML을 붙여넣습니다.

```text
본문 상단 광고
본문 중간 광고
본문 하단 광고
사이드바 광고
목록 상단 광고
목록 중간 광고
목록 하단 광고
모바일 하단 고정 광고
```

코드가 비어 있는 슬롯은 화면에 나오지 않습니다. 사이드바·본문 하단처럼 광고 코드가 들어 있는 슬롯은 광고가 채워지기 전까지 점선 `AD` 자리 표시가 보입니다.

중간 광고는 본문 두 번째 H2 뒤에 자동으로 옮겨집니다. 목록 중간 광고는 네 번째 글 뒤에 들어갑니다.

이 값은 테마 HTML 하단의 설정에서 바꿀 수 있습니다.

```js
window.sugarcoatConfig = {
  articleMiddleAdAfterHeading: 2,
  listAdAfterItem: 4,
  showAdPlaceholders: false,
  enableMobileStickyAd: false
};
```

- `showAdPlaceholders`: 운영 중에는 false
- `enableMobileStickyAd`: 처음에는 false. 정책과 이탈률을 본 뒤 true

자동 광고를 이미 쓰고 있다면 수동 슬롯을 한 번에 다 켜지 마세요. 본문 상단/하단부터 확인하고 중간/사이드바를 추가하는 편이 안전합니다.

레이아웃에서 가젯이 켜져 있는데 자리 표시조차 없으면, 예전 테마 파일을 쓰는 겁니다. 사이드바·본문 하단 광고는 숨긴 가젯에서 옮기지 않고 그 자리에 바로 렌더링합니다. 새 xml을 다시 붙여넣으세요.

광고가 아직 안 채워져도 점선 `AD` 박스는 보여야 합니다. 실제 광고가 뜨면 자리 표시는 사라집니다.

사이드바는 `300x250` / 슬롯 `7633913202`, 본문 하단은 `336x280` / 슬롯 `1008345394`가 맞습니다. 클라이언트 ID는 `ca-pub-6692939836499331`입니다.

---

## 메뉴와 라벨

상단 메뉴는 Blogger 페이지 목록 위젯입니다. 레이아웃 → 상단 메뉴에서 페이지를 추가합니다.

카테고리처럼 쓰고 싶은 분류는 글 라벨을 사용합니다. 사이드바 라벨 위젯이 그 목록을 보여줍니다.

추천 라벨 예:

```text
생활정보 / 정책 / IT / 리뷰
```

---

## 적용 후 확인

1. 시크릿 창으로 블로그 홈을 연다
2. 헤더에 easypress / 애드센스팜 문구가 없는지
3. 글 하나에서 제목, 본문, 목차, 댓글이 나오는지
4. 사이드바·본문 하단에 `AD` 자리 표시 또는 실제 광고가 나오는지
5. 모바일에서 메뉴와 목차 버튼이 열리는지
6. 네이버 서치어드바이저 / 애널리틱스가 계속 잡히는지
7. 광고 코드 없는 슬롯이 숨겨지는지

코드가 없는 슬롯은 숨겨져야 합니다. 코드가 있는 슬롯은 광고가 없어도 `AD` 자리 표시가 남아야 합니다.

문제 있으면 백업 파일 `blogger-backup-현재테마.xml`을 다시 복원하면 됩니다.

---

## 원본 파일

참고용 원본은 그대로 두었습니다.

```text
easypress_light_theme_v.1.2.5.xml
easypress_light_theme_v.1.2.5.zip
```

새 테마는 이 원본을 복제한 것이 아니라, 광고 슬롯 개념만 남기고 다시 만든 Blogger 테마입니다. 애드센스팜 크레딧, 쿠팡 자동삽입, 좌우 플로팅 배너는 넣지 않았습니다.
