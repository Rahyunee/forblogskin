# 티스토리 스킨 적용 가이드

## 적용 대상 파일

이 스킨은 아래 파일을 기준으로 적용합니다.

```text
skin.html
style.css
images/script.js
```

루트의 `script.js`는 작업/검토용 원본입니다. 티스토리에는 `images/script.js`를 파일 업로드하고, `skin.html`에서는 `./images/script.js`로 불러옵니다.

## 적용 전 백업

1. 티스토리 관리자에 접속합니다.
2. `꾸미기 > 스킨 변경 > 보관` 또는 현재 사용 중인 스킨 백업 기능으로 기존 스킨을 저장합니다.
3. 기존 HTML/CSS에 직접 넣은 광고 코드, 메타태그, 애널리틱스 코드가 있으면 별도로 복사해둡니다.

## 적용 방법

### 1. HTML 적용

1. `꾸미기 > 스킨 편집 > HTML 편집`으로 이동합니다.
2. `skin.html` 내용을 전체 복사합니다.
3. 티스토리 HTML 편집기의 기존 내용을 전체 선택 후 붙여넣습니다.

### 2. CSS 적용

1. 같은 화면의 `CSS` 탭으로 이동합니다.
2. `style.css` 내용을 전체 복사합니다.
3. 기존 CSS를 전체 교체합니다.

### 3. JS 파일 업로드

1. `파일 업로드` 탭으로 이동합니다.
2. `images/script.js` 파일을 업로드합니다.
3. 업로드 후 HTML 하단에 아래 코드가 있는지 확인합니다.

```html
<script src="./images/script.js" defer></script>
```

## 상단 GNB/카테고리 설정

현재 스킨의 상단 GNB는 티스토리 카테고리 치환자 `[##_category_list_##]`를 사용합니다.

즉, 상단 메뉴는 프리뷰처럼 `홈/본문/사이드바/광고영역`으로 쓰는 것이 아니라 실제 운영 카테고리로 보이게 됩니다.

관리 순서는 아래처럼 하면 됩니다.

1. 티스토리 관리자에서 `콘텐츠 > 카테고리 관리`로 이동합니다.
2. 상단에 노출하고 싶은 카테고리를 정리합니다.
3. 카테고리명이 너무 길면 모바일에서 줄바꿈이 생길 수 있으므로 짧게 유지합니다.
4. 하위 카테고리는 상단 GNB에서는 기본 숨김 처리하고, 사이드바 카테고리 영역에서 탐색하도록 둡니다.

추천 카테고리 예시:

```text
전체 / 지원금 / 금융 / 생활정보 / IT / 리뷰
```

## 광고 설정

`skin.html` 상단의 설정값을 먼저 확인합니다.

```html
<script>
  window.tistorySkinConfig = {
    adsenseClient: "",
    articleMiddleAdAfterHeading: 2,
    listAdAfterItem: 4,
    showAdPlaceholders: false,
    enableMobileStickyAd: false
  };
</script>
```

- `articleMiddleAdAfterHeading`: 본문 몇 번째 H2 뒤에 중간 광고 placeholder를 넣을지 결정합니다.
- `listAdAfterItem`: 목록 몇 번째 카드 뒤에 목록 중간 광고 placeholder를 넣을지 결정합니다.
- `showAdPlaceholders`: 광고 코드가 없을 때 광고 박스를 보여줄지 여부입니다. 운영 블로그에서는 `false` 권장입니다.
- `enableMobileStickyAd`: 모바일 하단 고정 광고 사용 여부입니다. 처음에는 `false` 유지 권장입니다.

## 애드센스 연결 전에는 무엇이 보이나요?

기본값이 아래처럼 되어 있으면 광고 코드가 없는 슬롯은 자동으로 숨겨집니다.

```js
showAdPlaceholders: false
```

따라서 애드센스 연결 전 운영 블로그에서는 빈 광고 박스가 보이지 않는 것이 정상입니다.

단, `preview.html`에서는 디자인 확인을 위해 `showAdPlaceholders: true`로 설정되어 있어서 광고 위치 placeholder가 보입니다. 이건 프리뷰 전용입니다.

## 애드센스 연결 후 설정 순서

티스토리 수익 메뉴에서 애드센스를 연결하면 아래 치환자가 자동 광고 영역으로 동작할 수 있습니다.

```text
[##_revenue_list_upper_##]
[##_revenue_list_lower_##]
[##_revenue_article_upper_##]
[##_revenue_article_lower_##]
```

권장 순서:

1. 티스토리 관리자에서 `수익 > 애드센스 관리`로 이동합니다.
2. 애드센스 계정을 연결합니다.
3. 우선 티스토리 기본 광고 위치 중 `본문 상단`, `본문 하단`부터 켭니다.
4. 블로그 화면에서 광고가 정상 노출되는지 확인합니다.
5. 며칠 운영 후 RPM/체류시간/이탈률을 보고 중간 광고나 사이드바 광고를 추가합니다.

수동 광고 코드를 넣고 싶다면 각 `.ad-slot` 영역 안에 AdSense 코드를 넣으면 됩니다. 단, 처음 적용 시에는 자동 광고와 수동 광고를 동시에 과하게 켜지 않는 것을 권장합니다.

수동으로 추가하기 좋은 위치:

```text
본문 중간 광고: article-middle-ad-template 내부
목록 중간 광고: list-ad-template 내부
사이드바 광고: ad-slot--sidebar 내부
모바일 하단 고정 광고: ad-slot--mobile-sticky 내부
```

수동 광고를 넣은 뒤에도 `showAdPlaceholders: false`는 그대로 두면 됩니다. 광고 코드가 들어간 슬롯은 숨겨지지 않고, 코드가 없는 슬롯만 자동으로 숨겨집니다.

## 적용 후 확인 체크리스트

1. 홈/목록 페이지
   - 글 카드가 1열 또는 2열로 정상 표시되는지 확인
   - 목록 중간 광고 영역이 너무 위에 붙지 않는지 확인

2. 글 상세 페이지
   - 제목, 날짜, 카테고리, 본문이 정상 표시되는지 확인
   - 목차가 H2/H3 기준으로 생성되는지 확인
   - 본문 상단/중간/하단 광고 영역이 밀림 없이 잡히는지 확인

3. 모바일
   - 메뉴 버튼이 열리고 닫히는지 확인
   - 검색창이 보이는지 확인
   - 목차 버튼이 동작하는지 확인
   - 광고가 본문을 과하게 가리지 않는지 확인

4. 운영 기능
   - 검색 결과 페이지로 이동되는지 확인
   - 링크 복사/공유 버튼이 동작하는지 확인
   - 댓글 영역이 보이는지 확인

## 이대로 적용해도 되는지

구조상 바로 적용 가능한 형태로 정리되어 있습니다. 다만 티스토리는 블로그 설정, 플러그인, 수익 메뉴 상태에 따라 치환자 출력이 달라질 수 있으므로 운영 블로그에 바로 반영하기 전에 테스트 블로그 또는 미리보기에서 먼저 확인하는 것을 권장합니다.

특히 아래는 적용 후 직접 확인해야 합니다.

- 티스토리 수익 치환자가 실제 광고 코드로 변환되는지
- 기존 애드센스 자동 광고와 수동 슬롯이 중복되지 않는지
- 기존 스킨에서 사용하던 별도 메타태그/애널리틱스/서치콘솔 코드가 빠지지 않았는지
- 사용 중인 댓글/관련글 플러그인과 충돌이 없는지


## 네이버 서치어드바이저/SEO 설정 가이드

### 소유확인 메타태그는 어디에 넣어야 하나요?

스킨 HTML에 직접 넣은 메타태그는 `skin.html`을 전체 교체할 때 사라질 수 있습니다. 가능하면 티스토리 관리자에서 제공하는 메타태그/플러그인 기능으로 관리하는 것을 권장합니다.

추천 순서:

1. `관리자 > 플러그인`에서 `메타 태그 등록` 기능이 있는지 확인합니다.
2. 가능하면 네이버 소유확인 태그를 플러그인에 등록합니다.
3. 플러그인 사용이 어렵다면 `skin.html`의 `[##_head_##]` 바로 아래 주석 위치에 직접 붙여넣습니다.
4. 이후 `skin.html`을 다시 교체할 때는 해당 메타태그를 한 번 더 확인해야 합니다.

예시:

```html
<meta name="naver-site-verification" content="발급받은_값">
```

### 네이버 진단 기준 추천 사이트명

현재처럼 긴 제목은 네이버에서 중복/과다 제목으로 볼 수 있습니다. 사이트명은 40자 이내, 가능하면 10~18자 정도가 좋습니다.

추천안:

```text
깜찌맘 육아노트
깜찌맘의 육아노트
육아는 처음이라
깜찌맘 육아정보
```

가장 추천:

```text
깜찌맘 육아노트
```

이유: 짧고, 브랜드와 주제가 같이 들어갑니다.

### 네이버 진단 기준 추천 사이트 설명

네이버는 사이트 설명을 80자 이내로 권장합니다. 현재 설명은 다소 길어서 줄이는 것이 좋습니다.

추천 설명:

```text
초보 부모를 위한 신생아 육아정보, 정부지원정책, 생활정보, 육아용품 비교를 정리합니다.
```

더 부드러운 버전:

```text
초보 부모가 기대어 볼 수 있는 신생아 육아정보와 정부지원정책, 육아용품 정보를 정리합니다.
```

더 검색형 버전:

```text
신생아 육아정보, 출산준비, 정부지원정책, 육아용품 비교를 쉽게 정리하는 육아 블로그입니다.
```

### Open Graph 제목/설명

Open Graph 제목과 설명도 티스토리의 사이트명/사이트 설명을 따라가는 경우가 많습니다. 따라서 위 사이트명과 설명을 관리자에서 먼저 정리하는 것이 가장 효과적입니다.

### 이번 스킨에서 반영한 SEO 수정

`skin.html`의 title 구조를 아래처럼 단순화했습니다.

```html
<title>[##_page_title_##]</title>
```

이렇게 하면 티스토리의 `[##_page_title_##]`이 이미 블로그명을 포함하는 경우 제목이 중복으로 붙는 문제를 줄일 수 있습니다.


## 관련 글과 목록 배지 운영 기준

### `함께 보면 좋은 글` 영역

이 스킨에 임시로 있던 `함께 보면 좋은 글` 박스는 제거했습니다. 티스토리 본문 하단에는 이미 `카테고리의 다른 글` 영역이 자동으로 노출될 수 있어서, 별도의 빈 추천 박스를 두면 중복처럼 보이고 설정 방법도 혼란스럽기 때문입니다.

관련 글을 운영하려면 아래 중 하나를 권장합니다.

1. 티스토리 기본 `카테고리의 다른 글` 영역을 그대로 사용
2. 본문 안에서 운영자가 직접 내부 링크를 2~3개 수동 삽입
3. 나중에 실제 추천 로직/플러그인을 쓸 때 별도 영역 추가

### 목록 카드 배지와 태그 노출

목록 카드의 `목록`, `카테고리 글` 같은 임시 배지는 제거했습니다. 현재 보고 있는 카테고리는 archive 헤더와 GNB/사이드바에서 이미 알 수 있기 때문에 카드마다 반복할 필요가 없습니다.

해시태그를 목록 카드에 2~3개 노출하는 방안은 검토했지만, 티스토리 공식 치환자 기준으로는 안전하지 않습니다.

- 글 상세: `<s_tag_label>` / `[##_tag_label_rep_##]` 사용 가능
- 글 목록/카테고리 목록: `s_list_rep` 안에서 글별 태그 치환자 제공 안 됨

따라서 태그는 글 상세 하단의 태그 영역에서만 노출하는 것이 안전합니다. 목록 카드에 태그를 억지로 넣으면 치환자가 그대로 보이거나 빈 값으로 깨질 수 있습니다.
