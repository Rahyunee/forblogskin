# Quietline Blogger 적용 가이드

Blogger(블로그스팟) 테마입니다. 티스토리 스킨이 아닙니다.

## 적용 파일

운영 블로그에 올리는 파일은 하나입니다.

```text
quietline_blogger_theme.xml
```

`preview.html`은 로컬에서 레이아웃만 보는 미리보기입니다.

## 적용 전 백업

1. Blogger 관리자 > 테마
2. 맞춤설정 > 백업 또는 테마 저장
3. 현재 xml을 다운로드해 둡니다
4. AdSense, 애널리틱스, 검색 메타가 테마 HTML에 직접 들어 있다면 따로 복사합니다

## 적용 방법

1. Blogger 관리자 > 테마 > 복원
2. `quietline_blogger_theme.xml` 업로드
3. 미리보기에서 홈/글/모바일 확인 후 저장
4. 레이아웃 메뉴에서 블로그 제목, 페이지 목록, 라벨이 맞는지 확인합니다

HTML 편집기에 붙여넣는 방식도 가능합니다. 테마 > 맞춤설정 > HTML 편집에서 전체 교체하면 됩니다.

## 광고 넣는 위치

테마 > 레이아웃에서 아래 가젯이 보입니다. 각 가젯에 AdSense HTML을 붙여넣습니다.

```text
본문 상단 광고
본문 중간 광고
본문 하단 광고
사이드바 광고
목록 상단 광고
목록 중간 광고
목록 하단 광고
모바일 하단 고정 광고
```

코드가 비어 있으면 해당 박스는 화면에 나오지 않습니다. 빈 AD 박스가 보이면 정상 운영 상태가 아닙니다.

중간 광고는 본문 두 번째 H2 뒤에 자동으로 옮겨집니다. 목록 중간 광고는 네 번째 글 뒤에 들어갑니다.

이 값은 테마 HTML 하단의 설정에서 바꿀 수 있습니다.

```js
window.quietlineConfig = {
  articleMiddleAdAfterHeading: 2,
  listAdAfterItem: 4,
  showAdPlaceholders: false,
  enableMobileStickyAd: false
};
```

- `showAdPlaceholders`: 운영 중에는 false
- `enableMobileStickyAd`: 처음에는 false. 정책과 이탈률을 본 뒤 true

자동 광고를 이미 쓰고 있다면 수동 슬롯을 한 번에 다 켜지 마세요. 본문 상단/하단부터 확인하고 중간/사이드바를 추가하는 편이 안전합니다.

## 메뉴와 라벨

상단 메뉴는 Blogger 페이지 목록 위젯입니다. 레이아웃 > 상단 메뉴에서 페이지를 추가합니다.

카테고리처럼 쓰고 싶은 분류는 글 라벨을 사용합니다. 사이드바 라벨 위젯이 그 목록을 보여줍니다.

추천 라벨 예:

```text
생활정보 / 정책 / IT / 리뷰
```

## 적용 후 확인할 것

1. 홈에서 최신 글 목록과 썸네일
2. 글 페이지에서 제목, 본문, 목차, 댓글
3. 검색과 라벨 보관함 페이지
4. 모바일 메뉴, 검색, 목차 버튼
5. 광고 코드 없는 슬롯이 숨겨지는지
6. 기존 애널리틱스/서치콘솔 소유확인 코드가 빠지지 않았는지

## 원본 파일

참고용 원본은 그대로 두었습니다.

```text
easypress_light_theme_v.1.2.5.xml
easypress_light_theme_v.1.2.5.zip
```

새 테마는 이 원본을 복제한 것이 아니라, 광고 슬롯 개념만 남기고 다시 만든 Blogger 테마입니다. 애드센스팜 크레딧, 쿠팡 자동삽입, 좌우 플로팅 배너는 넣지 않았습니다.
